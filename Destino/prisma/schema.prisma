
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model destino {
  id Int @id @default(autoincrement())
  Nombre String @unique 
  Coordenadas String
  fecha DateTime
  hora DateTime 
}
